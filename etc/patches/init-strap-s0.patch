move strap.sh to ttyS0/comport if specified in kargs
@@ -135,4 +135,4 @@
 				>> $sysroot/etc/inittab
-			# Baudrate of 0 keeps settings from kernel
-			echo "$tty::respawn:/sbin/getty -L 0 $tty $term" \
+			sed -ri '/strap.sh$/d' $sysroot/etc/inittab
+			echo "$tty::respawn:/bin/ash /etc/strap.sh" \
 				>> $sysroot/etc/inittab
